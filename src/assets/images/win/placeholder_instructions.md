# Windows Icon Resource Placeholders

The `src/assets/images/win/` folder contains your custom Windows icon files.

To customize the app icon for Windows, place your custom `.ico` file here:
- **Path**: `src/assets/images/win/icon.ico`

Tracking this markdown file ensures the directory structure is preserved in Git/GitHub even if the actual custom `.ico` file is not checked in yet.
